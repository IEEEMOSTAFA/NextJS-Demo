import React from 'react'

export default function About_layout({children}) {
  return (
    <div>
        {children}
      <p>This is the about layout page.it's only show when any one enter in the about folder</p>
    </div>
  )
}
